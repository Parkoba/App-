import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.parkoba',
  appName: 'Parkoba',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  },
  android:{

  }
};

export default config;
